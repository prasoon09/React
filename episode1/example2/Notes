Things learnt:
    1.How do we create H1 element inside react
    2.How do we give attributes to the tags using react
    3.How do we get css into our page.(basic)
    4.DOM manipulation is the most expensive operation in browser.
    5.React.createElement() : creates an object. This object is called ReactElement. 
                              Each html tag is represented as ReactElement in React World.
    6.ReactDOM.createRoot() : creates a root ReactElement.
    7.render() : responsible to take this object, convert it into respective html tag, 
                 and put it up on the DOM